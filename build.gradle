plugins {
    id 'java'
    id 'idea'
}
group = 'com.sqa'
version = '1.0-SNAPSHOT'
repositories {
    mavenCentral()
}
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(11)
    }
}
dependencies {
// ✅ Dependencia principal de Karate
    testImplementation 'com.intuit.karate:karate-junit5:1.2.0.RC5'
// (Opcional) Para generar reportes más detallados
    testImplementation 'net.masterthought:cucumber-reporting:5.7.0'
}
test {
    useJUnitPlatform()
// Permite ver logs de Karate en consola
    testLogging {
        events "passed", "skipped", "failed"
    }
}
tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}